# DENUDATA.IO — Tableau de bord matinal
# Copier en `.env` (déjà gitignoré) et renseigner. Ne jamais committer le `.env` réel.

# --- Vikunja (VPS Hostinger) ---
VIKUNJA_URL=https://vikunja.srv1335677.hstgr.cloud
VIKUNJA_API_TOKEN=
# Nom du projet Vikunja où sont créées les tâches ajoutées depuis le panneau "Idées de la nuit"
VIKUNJA_NIGHT_PROJECT=Inbox

# --- Google Calendar (à activer dans une prochaine étape) ---
# Chemin vers le fichier credentials OAuth (client Google Cloud) et vers le token
# généré après le premier consentement dans le navigateur.
GOOGLE_CALENDAR_CREDENTIALS=
GOOGLE_CALENDAR_TOKEN=
